version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: postgres-bp
    restart: always
    environment:
      - POSTGRES_DB=bank
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    ports:
      - "5432:5432"
 